function ajouter_agence()
{
	var nom_a = $("#nom_a").val();
	var niveau_a = 1; //Provisoire
	var coord_point_a = $("#coord_point_a").val();
	var coord_latlng_a = $("#coord_latlng_a").val();
	var id_agence 	= $("#id_agence").val();
	if (niveau_a != "" && nom_a != "" && coord_point_a != "" && coord_latlng_a != "") {
		$("#result").html(attente);
	    $.ajax({
	    url:base_url+'agence/ajouter', 
	    method:'POST', 
	    data:{id_agence:id_agence,niveau_a:niveau_a,nom_a:nom_a,coord_point_a:coord_point_a,coord_latlng_a:coord_latlng_a}, 
	    success:function(result){
	        $("#result").html(result);
			$("#nom_a").val('');
			$("#niveau_a").val('');
			$("#coord_point_a").val('');
			$("#coord_latlng_a").val('');
			$("#id_agence").val('');
			prov_name = "";
	        }
	    });
	}
	else
	{
		alert("Compléter le nom de l'agence");
	}
}
function agence_modif(id_agence, provname)
{
	$("#id_agence").val(id_agence);
	prov_name = provname;
}

function supprimerAgence(id_agence)
{
	if (confirm('Voulez-vous supprimer cette agence ?')) {
		$.ajax({
	    url:base_url+'agence/supprimer', 
	    method:'POST', 
	    data:{id_agence:id_agence}, 
	    success:function(result){
	    	$("#tr_"+id_agence).fadeOut();
	        }
	    });
	}
}