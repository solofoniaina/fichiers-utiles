function apercu_image(id_package, lien_p)
{
	$("#place_image").html(attente);
    $.ajax({
    url:base_url+'produit/apercu_image', 
    method:'POST', 
    data:{id_package:id_package,lien_p:lien_p}, 
    success:function(result){
        $("#place_image").html(result);
        }
    });
}